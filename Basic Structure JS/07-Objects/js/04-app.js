const product = {
    name: 'TV 20 inch',
    price: 300,
    available: false
}

// const name = product.name
// console.log(product.name)

// destructuring
const { name, price } = product
console.log(name)
console.log(price)